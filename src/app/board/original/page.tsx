// Original AkashTodo — pre-redesign, kept as a reference surface.
// Lives at /board/original so we can compare it side-by-side with
// the new launcher and decide what to port back.

import AkashTodo from "../../akash-todo";

export default function BoardOriginalPage() {
  return <AkashTodo />;
}
